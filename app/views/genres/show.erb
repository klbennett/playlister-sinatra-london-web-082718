<h1>   <%= @genre.name %>   </h1>

<!-- show songs      - show artists      - link to songs     - link to artists -->


<h2> Songs </h2>
<ul>
<% @genre.songs.each do |song| %>
    <li>
    <a href="../songs/<%= song.slug %>">  <%= song.name %> </a>
    </li>
<% end %>
</ul>


<h2> Artists</h2>


<ul>
  <% @genre.artists.each do |artist| %>
    <li>
      <a href="../artists/<%= artist.slug %>"> <%=  artist.name %> </a>
    </li>
  <% end %>
</ul>



<!-- genre needs to link to artists -->
